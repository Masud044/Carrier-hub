import React from 'react';
import { useLoaderData } from 'react-router-dom';

const JobDetail = ({jobdata}) => {
   
   // console.log(jobdata)
  // const{id,name,subtile} = jobdata;
    

    return (
        <div>
           
               
        </div>
    );
};

export default JobDetail;